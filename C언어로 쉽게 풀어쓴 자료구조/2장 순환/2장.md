# 2장 순환

## 순환

순환(recursion)이란 어떤 알고리즘이나 함수가 자기 자신을 호출하여 문제를 해결하는 프로그래밍 기법이다.

### 순환의 예

```c
int factorial (int n){
    if( n <= 1 ) return (1);
    else return (n * factorial (n-1) );
}
```

순환은 스택을 이용한다.

